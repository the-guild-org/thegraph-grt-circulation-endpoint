describe("main flow", () => {
  test("should return a valid response when timestamp is valid", () => {});
  test("should return a valid response when timestamp is not valid", () => {});
  test("should return a valid response when current timestamp is not provided", () => {});
});
